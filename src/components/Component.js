class Component {
    /**
     * Create a new component instance.
     *
     * @param {import("../Mix")} mix
     */
    constructor(mix) {
        this.mix = mix;
    }
}

module.exports = Component;
